{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "babel": "babel ES6 --out-dir lib",
    "babel:s": "babel ES6 --out-dir lib -s",
    "babel:w": "babel -w src --out-dir lib"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0"
  }
}
